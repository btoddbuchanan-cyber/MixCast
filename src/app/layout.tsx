import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "MixCast Studios | Next Generation Virtual Production",
  description:
    "MixCast is a proprietary virtual production capture solution for the Film/TV industry. Efficient, Flexible, Affordable.",
  openGraph: {
    title: "MixCast Studios | Next Generation Virtual Production",
    description:
      "Proprietary virtual production capture solution for Film/TV. Efficient, Flexible, Affordable.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
